static std::string nazwa_pliku;
ImGui::Text("Nazwa pliku");
ImGui::SameLine();
char buffer[256];
strcpy(buffer, nazwa_pliku.c_str());

if (ImGui::InputText("##nazwa_pliku", buffer, sizeof(buffer)))
{
    nazwa_pliku = buffer;
}
