n <- nrow(df)

# Pętla, która zamienia wartości poniżej odwróconej przekątnej na NA
for (i in 1:n) {
  for (j in 1:n) {
    if (i + j > n + 1) {  # Sprawdzamy, czy element jest poniżej odwróconej przekątnej i równy zero
      df[i, j] <- NA  # Zamieniamy na NA
    }
  }
}
