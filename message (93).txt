
#include <algorithm>
#include <random>

std::random_device rd;
std::mt19937 g(rd());
std::shuffle(generatedData.begin(), generatedData.end(), g);

logProgress("Dane w generatedData zostały przelosowane.");
